
1. 意图路由Agent（调度中心）入口总控，识别用户提问类型，分流到对应子 Agent；读取用户画像，个性化调整回复	总调度，串联全部功能
2. 南大知识库检索 Agent	RAG 检索公共资料库、官网同步数据；联网补全最新公告；回答标注来源；生成思维导图	问答互动拓展（检索 + 导图）
3. 校园陪伴闲聊 Agent	自主发起话题、学业情绪安抚、校园日常聊天；读取用户画像做个性化对话	自主聊天功能
4. 个人 Notebook 读写 Agent	操作数据库：新增笔记 / 错题、查询个人笔记、删除记录、导出笔记；维护用户画像表	资料库辅助读写（个人库）
5. 结果整合输出 Agent	汇总上游 Agent 输出，统一排版、补充格式、包装学长语气、最终回复用户	统一出口，标准化输出。
二、画布完整编排链路（两种执行流，Coze 可视化拖拽实现）
链路 A：学术查询 / 导图需求（串行流水线，对应execution::seq）
开始节点 → 意图路由 Agent → 知识库检索 Agent → 结果整合输出 Agent
输入传递：意图 Agent 把用户意图{{user_input}}传给检索 Agent；
检索 Agent 读取公共知识库 + 联网工具，产出带来源的资料 / 思维导图代码；
检索结果{{南大知识库检索Agent.output}}流入整合 Agent，润色成学长话术发给用户。
链路 B：记笔记 / 查错题需求（串行）
开始节点 → 意图路由 Agent → Notebook 读写 Agent → 结果整合输出 Agent
路由识别到「记录 / 导出 / 删除笔记」指令，分流到读写 Agent；
读写 Agent 调用 Database 节点执行增删改查 SQL；
数据库执行结果传入整合 Agent，反馈操作结果。
链路 C：日常闲聊 / 主动关怀需求（串行）
开始节点 → 意图路由 Agent → 校园陪伴闲聊 Agent → 结果整合输出 Agent
路由识别无学术 / 笔记需求，分流闲聊 Agent；
闲聊 Agent 读取user_profile用户画像，主动发起期末 / 保研节点提醒；
闲聊内容流入整合 Agent 统一输出。
并行补充：复杂综合提问（扇出并行execution::par）
用户同时问「保研条件 + 整理思维导图 + 保存重点」：
意图路由 Agent 同时分流检索 Agent + Notebook 读写 Agent并行执行，两个 Agent 输出全部汇总到整合 Agent 统一排版。
主动对话的设计：第一层：单轮对话内「柔性后置关怀」
第二层：跨会话记忆回访
第三层：定时场景化暖心推送
第四层：随机对话设计
